services:
  searxng:
    image: searxng/searxng:latest
    container_name: searxng
    restart: unless-stopped

    environment:
      - BASE_URL=https://searxng.homelab.zad.bd/
      - INSTANCE_NAME=Homelab SearXNG

    volumes:
      - searxng_config:/etc/searxng

    networks:
      - proxy

    labels:
      - "traefik.enable=true"

      # HTTPS router
      - "traefik.http.routers.searxng.rule=Host(`searxng.homelab.zad.bd`)"
      - "traefik.http.routers.searxng.entrypoints=websecure"
      - "traefik.http.routers.searxng.tls.certresolver=letsencrypt"

      # IMPORTANT: internal container port
      - "traefik.http.services.searxng.loadbalancer.server.port=8080"

      # Forward real client IP to SearXNG (bot detection requires it)
      - "traefik.http.routers.searxng.middlewares=searxng-forwardedheaders"

networks:
  proxy:
    external: true

volumes:
  searxng_config: